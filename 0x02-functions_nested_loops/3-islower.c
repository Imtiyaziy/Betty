#include "main.h"

/**
 *_islower - display point
 *@c: The character t9 print
 *
 *Return: 0 for success
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
