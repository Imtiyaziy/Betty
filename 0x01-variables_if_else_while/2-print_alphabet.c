#include <stdio.h>

/**
 *main _ display "Printing the alphabet in lower case followed by a new line"
 *Return: O and end program
*/
int main(void)
{
	char alphabet = 'a';
	while (a <= z)
	{
		putchar (alphabet);
		alphabet++
	}
	
	putchar ('\n');
	return (0);
}
